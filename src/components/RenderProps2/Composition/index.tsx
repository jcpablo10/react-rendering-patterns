// Exercise of composition:
// 1. Create a component called `Toggle` that manages a boolean state and provides a function to toggle it.
// 2. Create a parent component that uses the `Toggle` component and displays its state.